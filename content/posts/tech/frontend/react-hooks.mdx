---
title: React Hooks 入門指南
date: 2024-01-03
description: 介紹React Hooks的基本概念和使用方式
category: '技術文章'
tags: ['React', 'JavaScript', '前端']
image: '/projects/hot-now.jpg'
slug: react-hooks-guide
---

# React Hooks 入門指南

React Hooks 是 React 16.8 中引入的新功能，讓你可以在不編寫 class 的情況下使用 state 以及其他的 React 特性。

## 什麼是 Hooks？

Hooks 是一些可以讓你在函數元件裡「鉤入」React state 及生命週期等特性的函數。

## 常用的 Hooks

### useState

```javascript title="app.js"
import React, { useState } from 'react';

function Counter() {
    const [count, setCount] = useState(0);

    return (
        <div>
            <p>你點了 {count} 次</p>
            <button onClick={() => setCount(count + 1)}>點我</button>
        </div>
    );
}
```

### useEffect

```jsx
import React, { useState, useEffect } from 'react';

function Example() {
    const [count, setCount] = useState(0);

    useEffect(() => {
        document.title = `你點了 ${count} 次`;
    });

    return (
        <div>
            <p>你點了 {count} 次</p>
            <button onClick={() => setCount(count + 1)}>點我</button>
        </div>
    );
}
```

## 結論

Hooks 讓函數元件更加強大，也讓程式碼更加簡潔和可讀。
